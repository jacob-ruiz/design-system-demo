/** @jsx jsx */
import { jsx } from 'theme-ui';
import { Label as Element } from 'theme-ui';
import PropTypes from 'prop-types';

const Label = (props) => {
  return <Element {...props} />;
};

Label.propTypes = {};

Label.defaultProps = {};

export default Label;
